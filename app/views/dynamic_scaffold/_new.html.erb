<%= form_with model: @record, url: './create' do |form| %>
  <%@manager.forms.each do |elem|%>
    <% if elem.type? :hidden_field %>
      <%= elem.render(form, @record) %>
    <% else %>
      <div class="form-group">
        <label><%= elem.label %></label>
        <%= elem.render(form, @record) %>
        <%if elem.description?%>
          <small class="form-text text-muted"><%= elem.description %></small>
        <%end%>
      </div>
    <% end %>
  <% end %>
  <%= form.button t('dynamic_scaffold.save'), class: 'btn btn-outline-warning btn-warning' %>
<% end %>
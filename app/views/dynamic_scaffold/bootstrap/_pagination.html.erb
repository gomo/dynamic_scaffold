<%if dsconf.list.pagination %>
  <div class="dynamicScaffold-row">
    <%= paginate @records, {views_prefix: 'dynamic_scaffold/bootstrap'}.merge(dsconf.list.pagination.kaminari_options) %>
  </div>
<%end%>